# conf/experiment/scr_ph_rn50.yaml
# @package _global_
defaults:
  - override /model: PH_RN50_Scr
  - override /topo: pi
  - override /data: tiny_imagenet
  - override /optim: adamw

run_name: PH_Scratch

train:
  epochs: 100 
  batch_size: 128

topo:
  concat: false 
  tbs: normal 

model:
  hidden_size: 1024
  tbs: normal 

