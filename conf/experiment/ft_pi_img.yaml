# conf/experiment/ft_pi_img.yaml
# @package _global_
defaults:
  - override /model: pi_img
  - override /topo: pi
  - override /data: tiny_imagenet
  - override /optim: adamw

run_name: PH_Fine_tune

train:
  epochs: 100 
  batch_size: 128

model:
  hidden_size: 512
  tbs: normal 
  ft_attn: false

topo:
  concat: false 
  tbs: normal 
