vectorization: pi              # was --tv
dim: 1                         # was --topodim
concat: false                  # was --topodim_concat
tbs: normal                    # was --tbs (small/normal/large)
resolution: 64                 # was --res, but for PI it's 64x64 not 100
bandwidth: 5
max_norm: false                # was --maxNorm
